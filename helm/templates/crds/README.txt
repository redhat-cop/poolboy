These templates provide CustomResourceDefinitions with dynamic domains.
The operator domain can be set with values `.operatorDomain.name` in
order to support multiple independent deployments of Poolboy in a cluster.
